# watilde's dotfiles
here is my chest:
+ emacs
+ fish
+ tmux
+ vim
+ zshrc

## Install via chest
```
$ cd ~
$ npm install -g chest
$ git clone git@github.com:watilde/dotfiles.git .chest
$ chest open
```
