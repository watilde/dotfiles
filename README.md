# watilde's dotfiles
![screen shot](./screen.jpg)

## Install via chest
```console
$ cd ~
$ git clone git@github.com:watilde/dotfiles.git .chest
$ source .bashrc
$ chest open
$ cd .chest
$ npm install
$ npm run submodule-init
```
